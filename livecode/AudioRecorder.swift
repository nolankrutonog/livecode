

import SwiftUI

struct AudioRecorderView: View {
    @State var record = false
    
    
    var body: some View {
        VStack {
            Button(action: {
                
            }) {
                
            }
        }
    }
}
